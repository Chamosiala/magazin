query Product($id: Int!) {
  product(id: $id) {
    id
    createdAt
    updatedAt
    name
    desc
    descSnippet
    price
    SKU
    category
    discount {
      id
      name
      desc
      discountPercent
      createdAt
      updatedAt
    }
  }
}
